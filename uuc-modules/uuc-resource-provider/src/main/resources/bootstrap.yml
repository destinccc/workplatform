# Tomcat
server:
  port: 9333

# Spring
spring:
  application:
    # 应用名称
    name: uuc-resource-provider
  profiles:
    # 环境配置
    active: @profiles.active@

knife4j:
  enable: false
  production: true
  basic:
    # 默认设置false不开放文档, 设置为true鉴权后开放
    enable: false
    username: admin
    password: admin123
